## 11月24日
1. 如果只用jwt作为前后交互时用户信息的载体, ws发送信息时如何携带上发送者ID? ws应该是需要减少冗余信息来提升传输效率的. 
> 一个方案就是在建立完毕连接后, 服务端使用映射记录ws会话和用户id的关系.
2. 现在token的字符数较多, 可以通过一个简单的token进行传输, 实际session信息存放在redis中, 只是说一个将存储压力交给服务端, 一个将通信压力交给网络. 如何进行选型比较好?
> 对于通信压力更大的WS来说, 最好是选择WS压力较小的方案进行设计.

## 12月2日
1. WebSocket断线重连问题